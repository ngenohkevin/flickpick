// ==========================================================================
// Browse Pages Loading State
// Displayed during data fetching for browse pages (movies, tv, animation, anime)
// ==========================================================================

import { SkeletonBrowsePage } from '@/components/ui';

export default function BrowseLoading() {
  return <SkeletonBrowsePage />;
}
